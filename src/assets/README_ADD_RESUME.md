Place your resume PDF here so the site can serve it at the "Get resume" links.

Required filename: khushi_resume.pdf

Steps:
1. Export or save your resume as a PDF named `khushi_resume.pdf`.
2. Copy the file into `src/assets/` in the project root.
3. Rebuild / restart the dev server if it's running.

If you prefer the previous email behavior, tell me and I can revert the changes or add a fallback.